[pytest]
# Sans cette ligne, `pytest tests` echoue sur un ModuleNotFoundError alors que
# `python -m pytest tests` passe : la commande console n'ajoute pas le dossier
# courant au chemin d'import, l'invocation par module si. La commande donnee
# dans le README doit marcher telle quelle, pour tout le monde.
pythonpath = .
testpaths = tests
